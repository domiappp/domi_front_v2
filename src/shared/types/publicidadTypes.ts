export interface Publicidad {
    id?: number
    imagen?: string
    ruta?: string
    estado: number
    orden: number
    fecha_inicio?: Date
    fecha_fin?: Date
}

export interface CreatePublicidad {
    
}